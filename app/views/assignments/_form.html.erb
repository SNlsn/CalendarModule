<%= form_for(@assignment) do |f| %>
  <% if @assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% @assignment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :assignment_type %><br />
    <%= f.select(:assignment_type, assignment_type_options) %>
  </div>
  <div class="field">
    <%= f.label :starts_at_text, 'Starts at: ' %><br />
    <%= f.text_field :starts_at_text %>
  </div>
  <div class="field">
    <%= f.label :duration_hours %><br />
    <%= f.number_field :duration_hours %>
  </div>
  <div class="field">
    <%= f.label :duration_minutes %><br />
    <%= f.number_field :duration_minutes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
